---
ownership: seed
governs: product
activation: conditional
enforcement: example
decision_owner: project
side_effects: none
---

<!-- ============================================================================
Design Read（生成前定调表）—— devrules/workflows/design-read.md 的产物。
铁律：任何将产生新 UI 的任务（新项目 / 新页面 / 新大区块 / 重设计），
禁止跳过本表直接生成界面。表填不出来 = 还不该动手。

分级：
  - 新项目 / 重设计：九节全填。
  - 产品架构 applicability 为 required：先完成 product-architecture-brief.md；
    verdict 通过后再把已批准的产品结论摘入本表。
  - 单个新页面 / 新大区块：以页面视角填 §1-§7 + §9（§5 可精简为 1 个参考）。
  - token 微调 / 单组件状态修补：不需要本表，直接走 design-change.md。

输出去向：随任务/PR 提交。能力、流程、IA、导航和 surface 所有权进入
产品/IA 基线；视觉 token、组件语义、交互语言和视觉禁令进入 DESIGN.md。
Design Read 是门禁证据，不替代两类长期事实源。
============================================================================ -->

# Design Read：<项目 / 页面名>

## 0. 范围与前置产物
- 工作类型：<new_project / prd_to_ui / new_page / redesign / refactor / visual_change>
- 产品架构 applicability：<required / not_required>
- Applicability 理由：
- 产品架构产物位置：<docs/product/... / docs/design-reads/... / N/A>
- 产品 readiness：<ready / ready_with_reversible_assumptions / N/A>
- 受影响 ID：<REQ / CAP / FLOW / SURFACE / DEC>
- 产品/IA 基线位置：
- 重构模式（如适用）：<incremental / hybrid / full_redesign；默认 hybrid>

## 1. 这是什么产品
- 一句话定义（产品类型 + 解决什么问题 + 一个具体使用画面）：
- 产品阶段：<MVP 验证 / 成长期 / 成熟产品>（决定视觉投入与打磨深度）

## 2. 给谁用
- 目标用户与使用场景（谁、在什么时候、带着什么状态打开它）：
- 使用频率定性：<每天泡在里面的工作台 / 每周看几次的面板 / 偶尔配置一次 / 一次性浏览的落地页>
  （频率越高 → 密度越高、教学越少、效率越优先）

## 3. 核心任务（Top 3，动词开头）
1. <查看 / 创建 / 对比 / 导出 / 监控 …>
2. …
3. …
- 首屏必须直接服务任务 1；与三大任务无关的界面元素，默认删除而不是保留。

## 3.1 非 UI 内容 / 不做什么
- PRD 或需求里不应直接进入 UI 的内容（技术实现、算法、数据库、内部流程、运营背景）：
- 本轮明确不做的范围：

## 3.2 已批准的产品架构摘要
- 主流程：
- 高频支流程：
- 异常/权限/离线流程：
- IA 摘要：
- 一级导航及稳定用户目标：
- 横切能力及统一 scope：
- 当前 surface 的 primary task、入口、出口与状态：
- 本轮禁止改变的产品决策：

## 4. 信息密度定调
- 结论：<高密度可扫描 / 中密度平衡 / 低密度叙事>
- 依据（从 §2 频率与 §3 任务推导，不允许"看起来舒服"这类理由）：

## 5. 同类成熟产品参考（≥2，必须具体到"哪个界面的哪种做法"）
- <产品 A · 界面/模式>：借鉴 …；不借鉴 …
- <产品 B · 界面/模式>：借鉴 …
- 反例（明确不做成什么样）：…
<!-- 参照物要具体：「Linear 的 issue 列表密度与行内状态」「Stripe Dashboard 的数据卡与表格」
     「Notion 的空状态引导」。"像 Apple 一样简洁"不是参考，是形容词。 -->

## 6. 设计语言选型（对号入座，可混合但写明主基调）
| 产品类型 | 基调 | 典型特征 |
|---|---|---|
| SaaS / CRM / 后台 | 克制、高密度、可扫描 | 中性底 + 单一行动色；表格/列表为主体；13~15px 正文；次级操作 hover 才现身；零装饰 |
| 消费 / 品牌 / 营销 | 真实视觉资产、情绪驱动 | 真实图片/插画（禁止灰块占位）；大标题叙事与滚动节奏；品牌色敢用但有纪律 |
| 工具 / 编辑器 | 效率、状态反馈、操作路径 | 面板化布局；每个操作有即时反馈；键盘可达；状态（保存/同步/错误）常显可见 |
- 本项目主基调与理由：
- 混合说明（如营销页 + 后台并存，写明各自适用范围）：

## 7. 布局逻辑
- 页面骨架：<侧栏+顶栏工作台 / 单栏阅读 / 分屏编辑 / 长滚动叙事 …> + 选择理由：
- 首屏排布：用户 5 秒内应看懂「这是什么、我能做什么、从哪开始」——分别由哪个元素回答：

## 8. 色彩 / 字体 / 组件系统结论
- DESIGN.md：<沿用现有 / 新建（走 design-init-new-project）/ 需扩展：哪些 token、哪些组件>
- 色彩：语义位怎么用、点缀色只出现在哪（一句话边界）：
- 字体：层级数量、正文尺寸结论：
- 本次涉及组件清单 → 对照 DESIGN.md Components；缺的先走 design-new-component：

## 9. 反模板承诺（生成前逐项自查，签字画押）
- [ ] 界面中不会出现：技术实现说明、功能介绍段落、快捷键海报、内部机制描述、
      占位文案、无意义标签、AI 式营销废话（"强大的""一站式""重新定义"）
- [ ] 所有文案是"真实产品会写的话"：具体、面向任务；不是对界面自身的解说
- [ ] PRD 中的非 UI 内容不会被塞进页面解释文字；必要信息下沉到帮助/日志/后台
- [ ] 不用同权重卡片平铺凑布局、不做卡中卡；每个区块能回答"服务哪个核心任务"
- [ ] 图片/媒体位都有真实资产方案（真图 / 生成图 / 品牌插画），不留灰色占位块
- [ ] 首屏不堆 slogan + 三列 feature 卡的默认套路，布局由 §3 核心任务推导
- [ ] 产品门禁需要时已通过；本表没有私自改写能力归属、导航、流程或 surface scope
- [ ] 视觉结论能落到 DESIGN.md 的 token、组件语义、交互语言或明确待新增项
