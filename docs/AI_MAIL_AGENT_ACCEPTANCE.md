# Cloud Mail AI 邮件助手生产验收

验收开始：2026-07-15（Asia/Shanghai）  
观察周期：连续 7 天，由 Codex 自动化 `cloud-mail-ai-7` 每天 09:00 执行  
生产地址：`https://cloudmail.echoec.com`

本文只记录运行元数据，不记录邮件正文、附件、凭据、Token 或目标邮箱地址。

## 阶段门禁

| 阶段 | 结论 | 证据 |
|---|---|---|
| Phase 0：安全基础 | 通过 | 默认关闭、环境与数据库双开关、Workers AI binding、预算表和迁移均已验证 |
| Phase 1：站内安全预览 | 通过 | 仅选择 `privacytest@echoec.com`；1 次调用处理 4 封；4/4 项可追溯到源邮件 |
| Phase 2：调度与投递 | 通过，进入观察 | 规则每天 08:00（上海）运行；投递采用固定 Secret；一次显式投递成功且未重复推理 |
| Phase 3：长期运行 | 通过，进入观察 | 筛选、脱敏、截断、积压、保留/清理、告警、安全评测集、规则级调度/投递和投递熔断均已上线 |

## 2026-07-15 初始生产验收

- 初始投递验收 Worker 版本：`9b4a364a-6ecd-4db9-af30-9a5adb154342`；中文零成本观察配置版本：`835a31d3-0ba2-4c0c-800b-c798affefe4b`。
- 自动化测试：Vitest 4 个文件，41/41 通过。
- 前端：Vite release 构建通过，2,484 个模块完成转换。
- Wrangler：生产 dry-run 通过；D1、KV、R2、Workers AI、Assets、Send Email binding 均解析成功。
- HTTPS：200；HSTS、CSP、Permissions-Policy、Referrer-Policy、`nosniff` 均存在。
- 监控范围：唯一验收规则只映射 `privacytest@echoec.com`。
- 初始 AI 调用：1 次；估算输入 549 Tokens；估算 Neurons 44。完成性审计后，观察期生产熔断已收紧为 2 次/日、500,000 输入 Tokens、20,000 输出 Tokens、5,000 Neurons。
- 运行结果：`succeeded`；处理 4 封；积压 0；Prompt `digest-v1`。
- 来源隔离：摘要 4 个来源全部属于 `privacytest@echoec.com`，无跨邮箱来源。
- 投递：`sent`；尝试 1 次；投递不产生第二次模型调用。
- 中文回归：部署 `digest-v2-zh` 后执行一次仅站内安全预览，处理 4 封、积压 0；标题、总览与逐封摘要均为自然简体中文。该预览未请求投递、投递尝试为 0。
- 当日最终用量：2/2 次调用、1,195 输入 Tokens、4,096 输出 Tokens、89/5,000 Neurons；熔断阈值生效且未超限。
- 规则隔离发布：Worker `c0714d57-07bc-4c46-a28d-cd44f88b9a4e`；测试提升至 52/52。规则可独立配置半小时粒度时间、时区、语言和自动投递；生产规则保持 `08:00`、`Asia/Shanghai`、`zh-CN`，并已显式启用规则级自动投递。
- 推理恢复与来源链接发布：Worker `2105f407-6faa-4333-a031-9a4c8b4affab`；测试提升至 55/55。超时/5xx或非法结构化输出最多重试一次，且第二次调用必须原子预占预算；4xx及预算不足不重试。
- 摘要消息流发布：Worker `72fa101c-f5dc-4700-83ba-b6f36bd577d0`，提交 `ec88d4e`；测试提升至 57/57，前端 release 构建 2,486 个模块。生产管理台已验证按规则筛选返回 2 条、叠加高优先级筛选返回空状态、重置恢复 2 条；卡片显示时间窗口、邮件数、重要数和待办数，详情显示生成时间、模型与 Prompt 版本。
- 隐私安全运维指标发布：Worker `92ed27ff-76c6-4159-8b77-9d19b06c6e84`，提交 `74bf1e7`；测试提升至 59/59，前端 release 构建 2,488 个模块。生产指标为到期规则 0、成功/部分/失败/跳过 `2/0/0/0`、处理 8、过滤 0、JSON 校验失败 0、Provider 重试 0、AI D1 逻辑大小约 4 KiB；AI 不读取或写入 R2 附件。
- 可观测性迁移：`0003_ai_observability.sql` 在空数据库按 0001→0002→0003 应用成功，第二次执行无待迁移；生产应用后远端无待迁移。新增字段只保存有界错误类别、推理/校验/重试计数和每日逻辑存储快照，不保存错误消息或 Provider 原始响应。
- 新筛选 API 的未登录请求仍返回 401；规则 ID、邮箱 ID、日期及重要程度均经严格校验并使用 SQL 参数绑定，列表查询不读取邮件正文或 HTML。生产筛选验收前后用量保持 2/2 次、89/5,000 Neurons，未调用模型或投递邮件。
- 无推理数量预览：生产返回符合条件 4 封、本次处理 4 封、积压 0；调用后当日用量仍为 2/2、89 Neurons，证明未调用模型。
- 来源链接：生产 `https://cloudmail.echoec.com/ai-digest?digestId=…&emailId=…` 返回 200；在已登录管理台自动完成实时权限检查并落到当前 `/message` 源邮件详情。邮件模板测试确认 HTML/纯文本只包含该同源链接且无图片标签。
- 迁移：`0002_ai_monitor_soft_delete.sql` 在空数据库顺序应用及重复执行通过，生产应用成功且远端无待迁移；规则删除改为软删除，历史摘要保留。
- Gmail 收件端：Cloudflare 与 D1 均确认发送成功；当前可自动化 Gmail 会话不是目标账号，故本轮未重新取得收件箱视觉证据。既有基础验收已确认该 Cloudflare 验证目标可收件。
- 备份：初次发布前 D1 导出 27,796 字节，SHA-256 `253f599880c98188635157737dba6b3ea0df722a3eb873651817133f582f58c7`；规则隔离迁移前导出 39,938 字节，SHA-256 `c164ded3e06beab38eae14a2eda300e1e624830676692529bb01eac291b65ea9`；可观测性迁移前导出 40,146 字节，SHA-256 `45080aafdaa593b7c995ad99486d7502e0813d058dd17790a6bf3a64ec38abe4`。文件均位于受限临时目录且未加入 Git。

## 安全审计

- 普通用户不能访问 AI 管理 API；所有业务入口再次校验超级管理员身份。
- 18 个 AI API 的未认证集成测试均返回 401；所有 AI 服务边界在普通用户身份下于访问数据库前返回 403；全部 AI 写接口和预览接口均有独立速率限制。生产 `/api/ai/metrics` 未登录请求也返回 401。
- 摘要流筛选只返回摘要、运行元数据和计数；邮箱筛选通过来源关系做存在性检查，不把源邮件主题、正文或 HTML加入列表响应。
- 模型无工具、无附件、无数据库或外发权限，只接受清洗后的文本并返回受校验 JSON。
- 环境、数据库主开关、投递开关三层独立；投递关闭时禁止新投递及历史重试。
- 自动投递增加规则级第四层开关；规则未明确选择、被关闭或软删除时，不生成待投递摘要，也不重试历史待投递记录。
- 历史摘要来源在每次查看时重新检查源邮件、邮箱及所属用户仍有效；失效来源不会返回。
- 显式投递只读取 Cloudflare Secret `AI_DIGEST_DESTINATION_SECRET` 中的目标地址，请求体不能指定收件人；部署脚本还用本机忽略配置把 Send Email binding 限制到同一目标，临时配置部署后立即删除；每摘要最多 3 次尝试并受速率限制。
- `.env.local` 已被 Git 忽略；当前跟踪文件未发现私钥、API Key 或 Token 值。
- `pnpm audit` 仍因 npm 已退役的 audit 端点返回 HTTP 410；已改用 Google 官方 OSV Scanner 2.4.0。Windows AMD64 二进制 SHA-256 `0cdd113610126d5dfd5e12ad0e0b4f3e879291ff19bb43b0c52ed2f2c2df1a37` 与官方 GitHub Release 元数据一致；重新扫描 Worker 258 个包、前端 550 个包均为 `No issues found`。
- 目标邮箱地址曾出现在已推送历史提交中；当前版本已从跟踪配置和文档移除并改用 Secret。彻底清除历史需要单独批准历史重写与强制推送。
- 隐私收口发布后复核：Secret 名存在；生成的 Wrangler 临时文件已删除；Send Email binding 仍限制到单一脱敏目标；D1 保持 1 次成功运行、0 个重复窗口、1 次成功投递、4 个来源全部属于验收邮箱；基础邮件仍为 3 个活跃邮箱与 5 封有效邮件。
- 可观测性发布后只读复核：生产保持 1 条活跃规则，下一次运行 `2026-07-16T00:00:00.000Z`；重复窗口 0、越权来源 0、重试耗尽投递 0；当日用量仍为 2 次、1,195 输入 Tokens、4,096 输出 Tokens、89 Neurons。7 天自动化已更新为每日检查校验失败、Provider 重试、有界错误类别、D1 增长，并在第 7 天重新执行 OSV 扫描。
- AI 恢复演练：将可观测性迁移前生产备份导入隔离本地 D1，再成功应用 `0003_ai_observability.sql`。恢复结果为 2 个用户、4 个邮箱账号、5 封邮件、1 条监控规则、2 次运行、2 份摘要和 8 个来源；摘要→运行断链 0、来源→摘要/邮件断链 0，3 个运维列和 `ai_ops_daily` 表均存在。演练后已验证目标位于 `D:\Temp` 并删除临时明文数据库目录。
- 自动观察检查器：`scripts/verify-ai-observation.ps1` 以单条只读 D1 查询和 HTTPS 请求生成最小 JSON 证据，门禁失败时退出 1。首次生产执行通过：安全头 5/5、重复窗口/越权来源/重试耗尽/24 小时失败/超期规则均为 0，当日 2 次调用、89 Neurons；Wrangler 元数据确认 `rows_written=0`、`changed_db=false`。

## 连续 7 天记录

| 日期 | 成功/部分/失败 | 重复窗口 | 调用/Neurons | 投递 | 积压峰值 | 越权来源 | 正常收信抽检 | 结论 |
|---|---:|---:|---:|---|---:|---:|---|---|
| 2026-07-16（第 1 天） | 2/0/0 | 0 | 1 / 45 | 无新增投递异常 | 0 | 0 | 近 24 小时无新增邮件；活跃邮箱 3、卡在保存态 0 | 观察脚本通过；清除遗留明文投递目标变量，Worker v30；59/59 测试、前端构建、dry-run 与部署后复核通过 |
| 2026-07-15 | 2/0/0 | 0 | 2 / 89 | 1 次 sent；1 次仅站内 | 0 | 0 | 通过 | 中文 Prompt v2、零成本熔断、规则级调度/投递与无推理计数通过 |

第 7 天只有在以下条件全部满足时才能判定整体完成：无越权来源、无敏感正文日志、无重复窗口、每日预算未越线、投递重试未耗尽、AI 故障不影响正常邮件主链路，并完成最终依赖漏洞审计或明确记录其外部阻塞。

## 2026-07-17（第 2 天）生产只读观察

- 最小观察 JSON 通过：HTTPS 200、安全响应头 5/5、系统与投递开关开启、唯一活动规则 1 条；重复窗口、越权来源、重试耗尽、24 小时失败、推理 JSON 校验失败、Provider 重试和超期规则均为 0。
- 近 24 小时运行：成功 0、部分 0、跳过 1；积压峰值与总量均为 0。当天模型调用、输入/输出 Token 与 Neurons 均为 0，未产生 AI 推理成本。
- 调度与幂等：上海时间 08:00 的活动规则恰好 1 条，下一次运行时间为 2026-07-18 08:00；运行窗口无重复、周期边界有效，摘要来源游标无越界。无新增合格邮件时仅记录跳过，不重复推理。
- 正常邮件主链路：有效邮箱 3 个，近 24 小时新增有效邮件 0，孤立有效邮件 0；抽检未读取邮件正文、附件或个人信息。
- 线上 Worker：v30；AI、D1、KV、R2、Assets、邮件发送与 Secret 绑定存在，预算保持每天 2 次、500000 输入 Token、20000 输出 Token、5000 Neurons。只读 D1 查询元数据为 `rows_written=0`、`changed_db=false`。
- 结论：第 2 天验收通过，未发现需要修复或部署的缺陷；第 7 天 OSV Scanner 复扫仍按计划保留。

## 2026-07-18（第 3 天）生产只读观察

- 最小观察 JSON 通过：HTTPS 200、安全响应头 5/5、系统与投递开关开启、唯一活动规则 1 条；重复窗口、越权来源、重试耗尽、24 小时失败、推理 JSON 校验失败、Provider 重试和超期规则均为 0。首次完整脚本执行在 D1 请求处瞬时失败；同一只读 SQL 随即成功且完整脚本复跑通过，未发现持续性故障。
- 近 24 小时运行：成功 0、部分 0、跳过 1；积压峰值与总量均为 0。当天模型调用、输入/输出 Token 与 Neurons 均为 0，未产生 AI 推理成本。
- 调度与幂等：上海时间 08:00 的活动规则恰好 1 条，下一次运行时间为 2026-07-19 08:00；运行窗口无重复、周期边界有效，摘要来源游标无越界。无新增合格邮件时仅记录跳过，不重复推理。
- 正常邮件主链路：有效邮箱 3 个，近 24 小时新增有效邮件 0，孤立有效邮件 0；抽检只读取计数和关系元数据，未读取邮件正文、附件或个人信息。
- 线上 Worker：v30，100% 流量指向版本 `03afc174-acd6-4c2c-8d9c-20746f05ad7f`；AI、D1、KV、R2、Assets、邮件发送与 Secret 绑定存在，预算保持每天 2 次、500000 输入 Token、20000 输出 Token、5000 Neurons。只读 D1 查询元数据为 `rows_written=0`、`changed_db=false`。
- 结论：第 3 天验收通过，未发现需要修复、测试、构建、dry-run、提交、部署或推送的缺陷；第 7 天 OSV Scanner 官方 SHA-256 校验与双 `pnpm-lock.yaml` 复扫仍按计划保留。

## 2026-07-19（第 4 天）生产只读观察

- 最小观察 JSON：`{"observedAt":"2026-07-19T09:02:16.4245687Z","passed":true,"failures":[],"httpsStatus":200,"securityHeaders":5,"systemEnabled":1,"deliveryEnabled":1,"activeMonitors":1,"nextRunAt":"2026-07-20T00:00:00.000Z","duplicatePeriods":0,"unauthorizedSources":0,"exhaustedDeliveries":0,"failedRuns24h":0,"validationFailures24h":0,"providerRetries24h":0,"overdueMonitors":0,"callsToday":0,"inputTokensToday":0,"outputTokensToday":0,"neuronsToday":0}`。
- 最近 24 小时运行：成功 0、部分 0、跳过 1；积压峰值与总量均为 0。当日 AI 调用、输入/输出 Token 与 Neurons 均为 0，未产生 AI 推理成本。
- 调度与幂等：唯一活动规则精确匹配上海时间 08:00，下一次运行是 2026-07-20 08:00；重复周期 0、游标外来源 0。无新增合格邮件时本轮仅跳过，未重复推理。
- 正常邮件主链路：活动邮箱 3 个，最近 24 小时新增有效邮件 0，孤立有效邮件 0；抽检只读取计数与关系元数据，未读取邮件正文、附件或个人信息。
- 只读 D1 查询元数据：`rows_written=0`、`changed_db=false`。HTTPS、安全头、系统/投递开关、规则范围、失败/重试/超期门禁均通过。
- Cloudflare Worker 当前部署/版本元数据检查部分阻塞：`wrangler deployments status` 连续三次、`wrangler versions list` 一次均因代理链路 fetch 超时失败；同一时段生产 HTTPS 与 D1 API 可用，未发现授权失效证据。为避免把 2026-07-18 的 Worker v30/版本 ID 当作当前证据，本日不宣称版本与绑定复核完成。
- 结论：第 4 天业务与安全只读门禁通过，但 Worker 当前版本与绑定复核因 Cloudflare 元数据端点网络超时而部分阻塞。未发现代码缺陷，因此未运行测试、前端构建、dry-run，也未提交、推送或部署；第 7 天 OSV Scanner 官方 SHA-256 校验与双锁文件复扫仍按计划保留。

## 2026-07-20（第 5 天）生产只读观察

- 最小观察 JSON：`{"observedAt":"2026-07-20T09:03:20.6528504Z","passed":true,"failures":[],"httpsStatus":200,"securityHeaders":5,"systemEnabled":1,"deliveryEnabled":1,"activeMonitors":1,"nextRunAt":"2026-07-21T00:00:00.000Z","duplicatePeriods":0,"unauthorizedSources":0,"exhaustedDeliveries":0,"failedRuns24h":0,"validationFailures24h":0,"providerRetries24h":0,"overdueMonitors":0,"callsToday":0,"inputTokensToday":0,"outputTokensToday":0,"neuronsToday":0}`。
- 最近 24 小时运行：成功 0、部分 0、跳过 1；积压峰值与总量均为 0。当日 AI 调用、输入/输出 Token 与 Neurons 均为 0，未产生 AI 推理成本。
- 调度与幂等：唯一活动规则精确匹配上海时间 08:00，下一次运行是 2026-07-21 08:00；重复周期 0、运行周期结束后产生的来源 0。无新增合格邮件时本轮仅跳过，未重复推理。
- 正常邮件主链路：活动邮箱 3 个，最近 24 小时新增有效邮件 0，孤立有效邮件 0；抽检只读取计数与关系元数据，未读取邮件正文、附件或个人信息。
- Cloudflare Worker 当前为 v30，100% 流量指向版本 `03afc174-acd6-4c2c-8d9c-20746f05ad7f`；AI、D1、KV、R2、Assets、邮件发送与所需 Secret 绑定存在，预算保持每天 2 次、500000 输入 Token、20000 输出 Token、5000 Neurons。只读 D1 查询元数据为 `rows_written=0`、`changed_db=false`。
- 结论：第 5 天验收通过，第 4 天 Cloudflare 元数据端点超时阻塞已解除。未发现需要修复、测试、构建、dry-run、提交、部署或推送的缺陷；第 7 天 OSV Scanner 官方 SHA-256 校验与双锁文件复扫仍按计划保留。
